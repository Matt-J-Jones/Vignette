using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class guiChatResponse : MonoBehaviour
{
    // Start is called before the first frame update
    public void OnClick()
    {
        gameObject.SetActive(false);
    }
}
